#!/bin/sh
set -e

# Set up PulseAudio environment for snap confinement
if [ -f "$SNAP/bin/pulseaudio-setup" ]; then
    . "$SNAP/bin/pulseaudio-setup"
fi

export CORAOKE_CONFIG_DIR="$SNAP_USER_DATA/config"
export CORAOKE_SONGS_DIR="$SNAP_COMMON/coraoke-songs"

# When running as a daemon, redirect output to a log file.
# JOURNAL_STREAM is set by systemd for service units.
if [ -n "$SNAP_INSTANCE_NAME" ] && [ -n "$JOURNAL_STREAM" ]; then
    LOG_FILE="$SNAP_COMMON/coraoke.log"
    LOG_MAX=10485760  # 10 MB

    # Rotate the log if it exceeds the cap
    if [ -f "$LOG_FILE" ]; then
        log_size="$(stat -c '%s' "$LOG_FILE" 2>/dev/null || echo 0)"
        if [ "$log_size" -ge "$LOG_MAX" ]; then
            mv -f "$LOG_FILE" "${LOG_FILE}.1"
        fi
    fi

    exec >>"$LOG_FILE" 2>&1
fi

# Read snap configuration keys (set via: snap set coraoke key=value)
CORAOKE_ARGS=""

port="$(snapctl get port)" || true
if [ -n "$port" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --port $port"
fi

admin_password="$(snapctl get admin-password)" || true
if [ -n "$admin_password" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --admin-password $admin_password"
fi

download_path="$(snapctl get download-path)" || true
if [ -n "$download_path" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --download-path $download_path"
else
    CORAOKE_ARGS="$CORAOKE_ARGS --download-path $SNAP_COMMON/coraoke-songs"
fi

log_level="$(snapctl get log-level)" || true
if [ -n "$log_level" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --log-level $log_level"
fi

headless="$(snapctl get headless)" || true
if [ "$headless" = "true" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --headless"
fi

streaming_format="$(snapctl get streaming-format)" || true
if [ -n "$streaming_format" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --streaming-format $streaming_format"
fi

# Default to headless if no config was set for it
if [ -z "$headless" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --headless"
fi

proxy="$(snapctl get proxy)" || true
if [ -n "$proxy" ]; then
    CORAOKE_ARGS="$CORAOKE_ARGS --youtubedl-proxy $proxy"
fi

exec "$SNAP/bin/coraoke" $CORAOKE_ARGS "$@"
